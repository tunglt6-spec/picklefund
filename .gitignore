# Environment files — NEVER commit
# P0-1: .env.production KHÔNG còn được track (từng chứa JWT secret thật trên repo
# public — giá trị cũ coi như lộ, PHẢI rotate trên VPS). Chỉ example được commit.
.env
.env.*
!.env.example
!.env.production.example

# SSL certs
nginx/ssl/

# Node
node_modules/
dist/

# Test coverage (generated)
coverage/
backend/coverage/

# OS
.DS_Store
Thumbs.db
.claude/worktrees/

# desktop ad-hoc build verify (EPIC12) — never commit
desktop/dist-verify/
